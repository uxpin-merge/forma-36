import React from 'react';
import { Button } from '../Button';

export default (
  <Button
    uxpId="button-1"
    variant="primary"
    size="medium"
    // isActive={false}
    // disabled={false}
    type="default"
    // isDropdown={false}
    // icon={() => {}}
    // isLoading={false}
    isFullWidth={true}
  >
    Submit
  </Button>
);
